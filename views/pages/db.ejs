<!DOCTYPE html>
<html>
<head>
</head>

<body>


<div class="container">
<h2>Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.name %> - <%= r.ticker %> - <%= r.date %> - <%= r.open %> - <%= r.close %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
